{
  "name": "fwhp",
  "description": "Grant temp firewall access (or run other actions locally) through web authentication",
  "keywords": ["firewall", "auth", "authentication"],
  "version": "0.0.4",
  "homepage": "https://github.com/serjrd/fwhp",
  "repository": {
    "type": "git",
    "url": "https://github.com/serjrd/fwhp.git"
  },
  "bin": {
    "fwhp": "js/fwhp.js"
  },
  "scripts": {
    "postinstall": "mkdir -p ~/fwhp/ssl; cp config/config.js ~/fwhp && chmod 600 ~/fwhp/*"
  },
  "dependencies": {
    "yargs": "^1.3.1"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-coffee": "^0.11.1"
  }
}
